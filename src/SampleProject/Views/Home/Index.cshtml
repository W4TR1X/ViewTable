@model TableVM

@{
    ViewData["Title"] = "Home Page";
}

<div class="card">
    <div class="card-header">
        Test Table (Responsive & Vertical)
    </div>
    <div class="card-body">
        <view-table Table="@Model.TestTable"></view-table>
    </div>
</div>

<div class="card">
    <div class="card-header">
        System.Text.Json Serialize
    </div>
    <div class="card-body" style="height: 141px;overflow-y: auto;">
        @ViewBag.Json
    </div>
</div>

<div class="card">
    <div class="card-header">
        Test Table After Deserialize
    </div>
    <div class="card-body">
        <view-table Table="@ViewBag.modelFromJson"></view-table>
    </div>
</div>